<h1>Pssts: Secret Share Clubhouse</h1>
<h3>
    An exclusive clubhouse where members can write embarrassing posts about non-members. 
</h3>

<p>
    Inside the clubhouse, members can see who the author of a post is but, outside, they can 
    only see the story and wonder who wrote it. To be part of the members you must sign up.
</p>

<% if @signed_in %>
<h3>Logged in as: <%= @current_user.name %></h3>
<hr>
<p><%= link_to  "Create New Post", posts_new_path %></p>
<% end %>

<table class="table">
    <thead>
        <tr>
            <td>Title</td>
            <td>Body</td>
            <% if @signed_in %>
              <td>Author</td>
            <% end %>
            <td colspan="3"></td>
        </tr>
    </thead>

    <tbody>
        <% @posts.each do |post| %>
        <tr>
            <td><%= post.title %></td>
            <td><%= post.body %></td>
            <% if @signed_in %>
              <td>
                  <%= post.user.name%>
              </td>
            <% end %>
        </tr>
        <% end %>
    </tbody>
</table>

<hr>

<% if @signed_in %>
<%= link_to 'Sign Out', signout_path, method: :delete %>
<% else %>
<%= link_to 'Sign In', new_session_path  %> |
<%= link_to 'Sign Up', signup_path %>
<% end %>
